export const selectAllTeachers = (state) => state.teachers.items;
export const selectLastKey = (state) => state.teachers.lastKey;
export const selectTotalItems = (state) => state.teachers.totalItems;
